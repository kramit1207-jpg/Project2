Act as a Senior Full Stack AI Engineer. I need you to scaffold and generate code for a personality analysis application called "InsightProfile".

The app takes a LinkedIn URL, uses Humantic AI to get behavioral data, processes that data using a Google Gemini Agent (via LangChain), and displays a visual dashboard of the user's personality, strengths, and weaknesses.

### 1. Technology Stack
- **Frontend:** React (Vite), Tailwind CSS, Lucide React (Icons), Recharts (for visualization).
- **Backend:** Python, FastAPI.
- **AI/Orchestration:** LangChain, Google Gemini (via `langchain-google-genai`).
- **External API:** Humantic AI API.

### 2. Project Structure
Create a root directory with two folders:
- `/backend`: FastAPI service.
- `/frontend`: React app.

### 3. Backend Implementation Instructions (FastAPI)
- **Setup:** Create a virtual environment setup script.
- **Environment:** Use `python-dotenv` to load `HUMANTIC_API_KEY` and `GOOGLE_API_KEY`.
- **Endpoint:** Create a POST endpoint `/api/analyze`.
    - Input: JSON `{ "linkedin_url": "string" }`
    - Logic:
        1. Call Humantic AI API (assume an endpoint that accepts a LinkedIn URL or User ID and returns a JSON behavioral report including Big 5 or DISC scores). *Note: If specific Humantic SDK is unavailable, write a generic `requests` wrapper.*
        2. Initialize `ChatGoogleGenerativeAI` (Gemini Pro).
        3. Create a LangChain `PromptTemplate` that accepts the raw Humantic JSON.
        4. **The System Prompt:** "You are an expert Psychologist. Analyze the provided behavioral JSON data. Output a structured JSON response containing: 3 key Strengths, 3 key Weaknesses, and a brief 2-sentence summary of the personality."
        5. Return a combined JSON object to the frontend: `{ "analysis": <Gemini_Output>, "raw_scores": <Humantic_Numeric_Data> }`.
- **CORS:** Enable CORS for `localhost:3000` (Frontend).

### 4. Frontend Implementation Instructions (React)
- **UI Layout:** A clean, professional dashboard.
- **Input:** A central search bar for the LinkedIn URL.
- **Loading State:** specific UI component showing "Analyzing Behavioral Patterns..." while fetching.
- **Output Display:**
    - **Top Section:** The "Summary" text generated by Gemini.
    - **Middle Section (Split View):**
        - Left: A **Radar Chart** (using Recharts) mapping the raw personality scores (e.g., Openness, Conscientiousness, Extraversion, Agreeableness, Neuroticism).
        - Right: Two Cards. One for "Strengths" (Green accents) and one for "Weaknesses" (Red/Orange accents).
- **Styling:** Use Tailwind CSS for specific styling (Gradients, Shadows, Rounded corners).

### 5. Step-by-Step Execution Plan
1. Generate the folder structure.
2. Create the Backend `main.py`, `requirements.txt` (include `fastapi`, `uvicorn`, `langchain-google-genai`, `requests`, `python-dotenv`).
3. Create the Frontend components (`App.jsx`, `Dashboard.jsx`, `SearchInput.jsx`).
4. Provide the exact code for the LangChain chain logic.

Let's start by setting up the project structure and the Backend logic.